handlers = java.util.logging.ConsoleHandler
java.util.logging.ConsoleHandler.level=ALL

# CONFIG logs the registry HTTP requests/responses while hiding the
# Authorization header. Do NOT use ALL here: it would print the GHCR
# token into the (public) CI logs.
com.google.api.client.http.level=CONFIG
